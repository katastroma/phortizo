name: PR

on:
  workflow_dispatch:
  pull_request:
    branches: [default]

jobs:
  cog-check:
    name: CI
    uses: katastroma/skeuotheke/.github/workflows/cog-check.yaml@v0

  version-check:
    name: CI
    uses: katastroma/skeuotheke/.github/workflows/version-check.yaml@v0

  go-ci:
    name: CI
    uses: katastroma/skeuotheke/.github/workflows/ci-go.yaml@v0
